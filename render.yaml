services:
  - type: web
    name: hdm-shop-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot_shop.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0